export GRAPHICS_ROOT="$CPUROOT/usr/lib/graphics/jacinto6"
# Leaving the SATA driver in the list of apps to keep.  Has no effect on sd-only systems since this is just a list
# of processes that downsize should keep running.  It doesn't matter if it isn't there.
UPDATE_APPS_TO_KEEP="$UPDATE_APPS_TO_KEEP devb-ahci-omap5 tiler devb-sdmmc-omap_generic"
